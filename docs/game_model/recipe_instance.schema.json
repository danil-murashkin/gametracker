{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gametracker.local/schemas/recipe_instance.schema.json",
  "title": "Gametracker Recipe Instance (NFC item tag, max 512 bytes)",
  "type": "object",
  "required": [
    "scenario_name",
    "name",
    "uses_applied",
    "uses_left",
    "signature"
  ],
  "additionalProperties": false,
  "description": "Payload written to NFC user memory. Must fit in 512 bytes. Compact JSON only (no whitespace, no $schema). UID is read from the chip, not stored in JSON.",
  "properties": {
    "scenario_name": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*$",
      "maxLength": 16,
      "description": "Game scenario id. PDA rejects if != character.scenario_name"
    },
    "name": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*$",
      "maxLength": 32,
      "description": "Recipe name from scenario"
    },
    "uses_applied": {
      "type": "integer",
      "minimum": 0,
      "maximum": 65535
    },
    "uses_left": {
      "type": "integer",
      "minimum": 0,
      "maximum": 65535
    },
    "signature": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "HMAC-SHA256 hex (64 chars). UID for signing is taken from the NFC chip"
    }
  }
}
